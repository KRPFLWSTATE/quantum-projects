# GitHub Release notes (`v1.0.0-manuscript`)

This file is the release-notes body for a tagged snapshot. Creating the annotated git tag and opening a GitHub Release from it is an explicit repository-owner action and is not performed by this documentation update.

When the owner is ready:

1. Review uncommitted diffs; do not include `.env`, `venv/`, or IBM tokens.
2. Create an annotated git tag on the reviewed commit (for example `v1.0.0-manuscript`).
3. Open a GitHub Release from that tag using the notes below.
4. If Zenodo GitHub integration is later enabled by the author, a DOI can be minted from the Release. This file does **not** enable the integration or mint a DOI.

Suggested notes:

- Six completed `ibm_fez` decision-study SamplerV2 jobs and twenty GHZ probes.
- Offline reproduction: `python tools/reproduce.py`.
- Campaign complete; do not reopen the published ledger.

References: GitHub citation files; GitHub Python CI; GitHub/Zenodo software archival.
